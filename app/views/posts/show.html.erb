<div class="row">
  <div class="col">
    <h1><%= @post.title %></h1>

    <p><%= @post.body %></p>
    <p>&mdash;<%= @post.user.name %></p>

    <%= link_to 'Edit', edit_post_path(@post) %> |
    <%= link_to 'Back', posts_path %>
  </div>
</div>